<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="Lve">

<select id="leavelist" parameterType="ssg.com.maeil.dto.LeaveDto" resultType="ssg.com.maeil.dto.LeaveDto">
	select seq, employee_id, leave_start, leave_end, leave_type, leave_period, status
	from maeil_leave_list
	where employee_id = #{employee_id}
</select>

<insert id="leaveaddAf" parameterType="ssg.com.maeil.dto.LeaveDto">
	insert into maeil_leave_list(seq, employee_id, leave_start, leave_end, leave_type, leave_period, status)
	values(#{seq}, #{employee_id}, #{leave_start}, #{leave_end}, #{leave_type}, #{leave_period}, #{status})
</insert>

<delete id="leavedel" parameterType="java.lang.Integer">
	delete from maeil_leave_list
	where seq=#{seq}
</delete>

</mapper>